# Copy this file to .env and fill in your key, OR just export the variable in
# your shell. Get a FREE key (no credit card) at:
#   https://aistudio.google.com/apikey
#
# Note: .env is gitignored so your real key never gets committed.
# Leave this template's value EMPTY - it is committed to the repo.
GEMINI_API_KEY=
